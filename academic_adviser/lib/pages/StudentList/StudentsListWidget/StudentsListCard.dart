import 'package:flutter/material.dart';

class StudentsListCard extends StatelessWidget {
  const StudentsListCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
